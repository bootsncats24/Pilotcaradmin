# Repo root IS the marketing site (Pilotcaradmin). Do not set base — there is no marketing-website/ subfolder on Netlify.
[build]
  command = "npm run build"
  publish = ".next"
[[plugins]]
  package = "@netlify/plugin-nextjs"
